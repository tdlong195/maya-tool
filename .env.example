# VITE_GEMINI_API_KEY: Required for Gemini AI API calls from the browser.
# Note: Vite exposes VITE_* variables to the frontend bundle.
# For production secrecy, route Gemini calls through a backend proxy instead.
VITE_GEMINI_API_KEY="MY_GEMINI_API_KEY"

# Supabase: optional remote database. If omitted, the app falls back to browser localStorage.
VITE_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_PUBLIC_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"
